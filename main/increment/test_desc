# -*- python -*-

todo = Platform('caba', 'testbench.cpp',
        uses = [
				Uses('caba:interpolation'),
				Uses('caba:increment'),
				Uses('caba:increment_hard'),
				Uses('caba:buffer'),
        ],
        wb_data_width = 32,
        wb_addr_width = 32,
        )

